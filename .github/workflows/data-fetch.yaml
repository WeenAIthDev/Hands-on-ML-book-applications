on: 
  workflow_dispatch

jobs:
   dataset-fetch:
      runs-on: ubuntu-latest
      steps:
        - name: Print a greeting
          shell: python
          run: |
               import os 
               print(os.environ['PATH'])
